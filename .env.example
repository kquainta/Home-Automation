# Copy this file to .env in the project root (same folder as docker-compose.yml) and set real values. Do not commit .env.
# - Local run: backend loads this file from the project root.
# - Docker: .env is mounted into the backend container; create .env before "docker-compose up" or the mount may fail.

# Seed users (for E2E tests and optional admin login). Only used when E2E_SEED_USER=true or when calling POST /api/v1/auth/dev/seed-e2e-user.
E2E_SEED_EMAIL=
E2E_SEED_PASSWORD=
ADMIN_SEED_EMAIL=
ADMIN_SEED_PASSWORD=

# Set to true to create the above users on backend startup (e.g. after reload).
# E2E_SEED_USER=false

# Home Assistant (optional). Leave empty to disable dashboard integration.
# Create a long-lived token in HA: Profile → Long-Lived Access Tokens.
# HOME_ASSISTANT_URL=http://homeassistant.local:8123
# HOME_ASSISTANT_TOKEN=
